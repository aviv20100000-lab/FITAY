import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // אפשר להפעיל זמנית מפות מקור בעת חקירת תקלה בפרודקשן, ואז לכבות שוב.
  productionBrowserSourceMaps: false,

  // בלי זה, תגי ה-<script> של הצ'אנקים לא נושאים crossorigin="anonymous".
  // ב-Safari (בעיקר PWA במצב standalone באייפון, שכל המתאמנים משתמשים בו)
  // זה גורם לדפדפן להתייחס לשגיאות מהצ'אנקים כאילו הגיעו ממקור זר ולמחוק
  // מהן כל תוכן — ה-DeveloperErrorReporter מקבל רק "Script error." בלי
  // הודעה או שורת קוד אמיתית. Vercel כבר שולח Access-Control-Allow-Origin
  // על נכסים סטטיים, אז הצד היחיד שחסר הוא התג הזה.
  crossOrigin: "anonymous",

  experimental: {
    /**
     * כמה זמן המסך שכבר נטען נשמר בטלפון.
     *
     * ברירת המחדל היא אפס, כלומר חזרה לאותו טאב שולפת הכל מהשרת מחדש
     * בכל פעם. זו הסיבה שמעבר בין לשוניות הרגיש כבד גם אחרי שכבר היית שם.
     *
     * dynamic חל על ניווט רגיל, למשל כפתור חזרה או קישור בתוך מסך.
     * static חל על מסכים שנטענו מראש, וזה מה שקורה בשלוש הלשוניות.
     *
     * המשמעות: מסך שנפתח פעם אחת נפתח מיד בפעם הבאה, בלי רשת בכלל.
     * המחיר הוא שהנתונים יכולים להיות מיושנים עד חמש דקות. בפועל זה לא
     * מורגש, כי אחרי כל שמירה הקוד קורא ל-router.refresh ומאפס את השמירה.
     */
    staleTimes: {
      dynamic: 30,
      static: 300,
    },
  },

  // החבילה בונה את הנתיב לבינארי בזמן ריצה, עם require על מחרוזת מחושבת.
  // ה-bundler מנסה לפענח את זה בזמן בנייה, לא מצליח, והבנייה נופלת על
  // "Module not found: Can't resolve <dynamic>". כאן אומרים לו להשאיר
  // אותה בחוץ ולטעון אותה כרגיל בזמן ריצה.
  serverExternalPackages: ["@ffmpeg-installer/ffmpeg"],

  // ביטוח על אריזת הבינארי של ffmpeg.
  //
  // נבדק: המעקב האוטומטי כן אורז אותו גם בלי ההגדרה הזאת, כי החבילה
  // מסומנת חיצונית למעלה. ההגדרה נשארת בכל זאת, כי פעמיים כבר קרה
  // שהבינארי לא הגיע לפרודקשן והדחיסה נפלה שם בלבד. אריזה כפולה של
  // אותו קובץ לא מזיקה, וכשל בפרודקשן כן.
  //
  // התבנית מכסה את כל מסלולי ה-API, כדי שמסלול חדש לא יישכח.
  // linux-x64 היא החבילה שוורסל מריץ, win32 היא למחשב המקומי.
  //
  // הבנייה מדפיסה אזהרה על "whole project was traced". נבדק: היא מגיעה
  // מה-require הדינמי שבתוך @ffmpeg-installer, לא מההגדרה הזאת, והיא
  // מופיעה גם בלעדיה. החבילה נשארת 361 קבצים, כלומר שום דבר לא נגרר.
  outputFileTracingIncludes: {
    "/api/**": [
      "./node_modules/@ffmpeg-installer/linux-x64/**",
      "./node_modules/@ffmpeg-installer/win32-x64/**",
    ],
  },
};

export default nextConfig;
